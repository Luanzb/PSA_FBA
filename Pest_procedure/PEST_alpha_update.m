function [gabor] = PEST_alpha_update(gabor,PEST)

    % if info.first_row(trls.matrix(t,2),sub.ses_num) == t
    %     info.targ_alpha(t) = .5;
    % else
         gabor.contrast = PEST.xCurrent;
    % end

end